stages:
    -build
    -test

build:
    stage: build
    tags: 
        - docker.cs.dal.ca
    script:
        -mvn clean package -DskipTests=true

test:
    stage: test
    tags:
        - docker.cs.dal.ca
    script:
        -mvn clean verify
