stages:
    -build
    -test

build:
    stage: build
    tags: 
        - ugrad
    script:
        -cd Group20
        -mvn clean package -DskipTests=true

test:
    stage: test
    tags:
        - ugrad
    script:
        -cd Group20
        -mvn clean verify

